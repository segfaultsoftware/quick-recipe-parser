<div class="mx-auto md:w-2/3 w-full">
  <%= render "breadcrumbs", crumbs: [["Recipes", recipes_path], [@recipe.name, recipe_path(@recipe)], ["Edit", nil]] %>

  <h1 class="font-bold text-4xl mb-6">Edit Recipe</h1>

  <div class="bg-white shadow rounded-lg p-6">
    <%= render "form", recipe: @recipe %>
  </div>
</div>
